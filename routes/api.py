# from flask import Blueprint, jsonify, request
# from . import routes
#
# routes.url_prefix = '/api-file'
#
# @routes.route('/hello', methods=['GET', 'POST'])
# def hello():
#     if (request.method == 'POST'):
#         return jsonify({'status': 'ok', 'msg': 'Hello there!', 'method': 'POST'})
#     else:
#         return jsonify({'status': 'ok', 'msg': 'Hello there!', 'method': 'GET'})
